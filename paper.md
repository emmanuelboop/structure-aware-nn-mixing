# Structure-Aware Nearest Neighbor Mixing

We propose a data augmentation method that combines nearest neighbor selection with structure-aware mixing.

Unlike existing methods that rely on random pairing, we:
1. Select semantically similar samples
2. Identify meaningful structure regions
3. Mix only overlapping structure

This preserves both semantic and spatial consistency.

Experiments in progress.
